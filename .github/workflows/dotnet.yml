name: .NET Console App CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'
    
    - name: Restore dependencies
      run: dotnet restore src/ConsoleApp/ConsoleApp.csproj
    
    - name: Build
      run: dotnet build src/ConsoleApp/ConsoleApp.csproj --no-restore
    
    - name: Run application
      run: dotnet run --project src/ConsoleApp/ConsoleApp.csproj --no-build
